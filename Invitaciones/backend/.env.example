# Database Configuration
DATABASE_URL=postgresql://postgres:password123@localhost:5432/invitaciones_db

# JWT Configuration
SECRET_KEY=tu_clave_secreta_super_segura_cambiar_en_produccion
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Email Provider (smtp, aws_ses, sendgrid)
EMAIL_PROVIDER=smtp

# SMTP Configuration (para Gmail, Outlook, etc)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_USER=tu_email@gmail.com
EMAIL_PASSWORD=tu_contraseña_o_app_password

# AWS SES Configuration (si usas AWS)
AWS_ACCESS_KEY_ID=tu_access_key
AWS_SECRET_ACCESS_KEY=tu_secret_key
AWS_REGION=us-east-1

# SendGrid Configuration (si usas SendGrid)
SENDGRID_API_KEY=tu_sendgrid_api_key

# App Configuration
APP_NAME=Sistema de Invitaciones
VERSION=1.0.0
DESCRIPTION=API para gestión de invitaciones y confirmaciones de eventos

# CORS Origins (separados por coma)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://tu-dominio.com
